[package]
name = "atilla-agent"
description = "Rust mirror of @earendil-works/pi-agent-core — the agent loop and harness (scaffold)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
