//! Deferred port of pi's `utils/windows-self-update.ts`.
//!
//! Windows self-update quarantines native addons using `process.report` and
//! filesystem renames specific to the Node runtime on Windows. Deferred until a
//! Rust self-update strategy is defined.
