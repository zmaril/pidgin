[package]
name = "atilla-core"
description = "The atilla engine — the library the CLI drives."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
anyhow = { workspace = true }
